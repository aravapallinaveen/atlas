# Which backend the agent reasoning step runs on.
#   gateway    -> Butterbase AI gateway (Phase 1 default)
#   rocketride -> RocketRide Cloud pipeline (Phase 2)
# The front-end only reads this to decide which askAgent path to hit;
# it never changes the front-end contract.
VITE_AGENT_BACKEND=gateway
